<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pocandroidanimjetpackcompose">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POCAndroidAnimJetpackCompose">
        <activity android:name=".ActivityEvaluatorColor"></activity>
        <activity android:name=".ActivityTranslation" />
        <activity android:name=".ActivityCrossFade" />
        <activity android:name=".ActivityOpacity" />
        <activity android:name=".ActivityTransitionKeyframes" />
        <activity android:name=".ActivityScalling" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".ActivityRotate" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.POCAndroidAnimJetpackCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>